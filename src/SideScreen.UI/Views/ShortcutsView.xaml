<UserControl x:Class="SideScreen.UI.Views.ShortcutsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource Card}">
        <StackPanel>
            <TextBlock Text="Shortcuts" FontSize="16" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Note}" Style="{StaticResource MutedText}" Margin="0,4,0,12" TextWrapping="Wrap"/>
            <ItemsControl ItemsSource="{Binding Rows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Action}"/>
                            <Border Grid.Column="1" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="6" Padding="10,4">
                                <TextBlock Text="{Binding Gesture}" FontFamily="Consolas"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="Captura, conflitos e double-press entram na Fase 4." Style="{StaticResource MutedText}" Margin="0,12,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
